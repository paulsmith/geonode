~ geonode ~

Brings geography to node.js.

    var geonode = require("geonode");

    var geom = new geonode.Geometry();
    geom.fromWkt("POINT(1 2)");
    sys.puts(geom.toWkt());

    var geom2 = new geonode.Geometry();
    geom2.fromWkt("POLYGON((0 0, 2 0, 2 2, 0 2, 0 0))");
    sys.puts(geom2.covers(geom));

Installation
------------

    $ git clone git://github.com/paulsmith/geonode.git
    $ cd geonode
    $ node-waf configure build
    $ ln -s build/default/_geonode.node

More tk ...
